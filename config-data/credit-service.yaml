server:
  port: 5004
spring:
  application:
    name: credit-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  data:
    mongodb:
      host: localhost
      port: 27017
      database: creditBank