server:
  port: 5006
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: currencyBank
eureka:
    client:
        fetchRegistry: true
        registerWithEureka: true
        serviceUrl:
          defaultZone: http://localhost:8761/eureka