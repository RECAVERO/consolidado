server:
  port: 5008
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: purseBank
eureka:
    client:
        fetchRegistry: true
        registerWithEureka: true
        serviceUrl:
          defaultZone: http://localhost:8761/eureka