server:
  port: 5002
spring:
  application:
    name: type-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  data:
    mongodb:
      host: localhost
      port: 27017
      database: typeBank